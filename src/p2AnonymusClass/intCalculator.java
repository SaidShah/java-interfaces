package p2AnonymusClass;

// interfaces are classes where all the methods are public and they are unImplmented
public interface intCalculator {
	int calculate(int number);             // by default all methods are public in interfaces
								          // so you don't need to write public when creating the method
	int compute(int num1, int num2);
}
